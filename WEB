// Index.html 

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Portal | Elite Academy</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #132743;
            --secondary-color: #2f3f60;
            --accent-color: #b59a3b;
            --accent-light: #d4b757;
            --text-light: #f8f9fa;
            --text-dark: #212529;
            --error-color: #a3320b;
            --surface-color: #ffffff;
        }

        @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Poppins:wght@300;400;500;600&display=swap');

        body {
            background-image: url('/api/placeholder/1200/800');
            background-size: cover;
            background-position: center;
            font-family: 'Poppins', sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
            position: relative;
        }

        /* Luxury overlay */
        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(19, 39, 67, 0.92) 0%, rgba(47, 63, 96, 0.85) 100%);
            z-index: 0;
        }

        .login-container {
            position: relative;
            z-index: 2;
            display: flex;
            width: 850px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
            border-radius: 8px;
            overflow: hidden;
        }

        .login-image {
            flex: 1;
            background-image: url('/api/placeholder/400/600');
            background-size: cover;
            background-position: center;
            position: relative;
        }

        .login-image-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(19, 39, 67, 0.4);
            display: flex;
            flex-direction: column;
            justify-content: flex-end;
            padding: 30px;
        }

        .login-quote {
            color: white;
            font-family: 'Playfair Display', serif;
            font-size: 24px;
            font-style: italic;
            margin-bottom: 15px;
            line-height: 1.4;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        }

        .quote-author {
            color: var(--accent-light);
            font-size: 16px;
            font-weight: 500;
        }

        .login-form-container {
            flex: 1;
            background-color: var(--surface-color);
            padding: 50px 40px;
            display: flex;
            flex-direction: column;
        }

        .login-logo {
            text-align: center;
            margin-bottom: 30px;
        }

        .logo-image {
            display: inline-block;
        }
        
        .login-logo i {
            font-size: 2.8rem;
            color: var(--accent-color);
            background: linear-gradient(45deg, var(--primary-color), var(--accent-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .login-header {
            margin-bottom: 40px;
        }

        .login-header h2 {
            font-family: 'Playfair Display', serif;
            color: var(--primary-color);
            font-weight: 700;
            font-size: 2.2rem;
            margin-bottom: 10px;
            text-align: center;
        }

        .login-header p {
            color: #6c757d;
            font-size: 0.9rem;
            text-align: center;
        }

        .form-group {
            margin-bottom: 25px;
            position: relative;
        }

        .form-control {
            border-radius: 4px;
            padding: 14px 20px 14px 50px;
            background: #f8f9fa;
            border: 1px solid #e9ecef;
            transition: all 0.3s ease;
            font-size: 1rem;
            color: var(--text-dark);
        }

        .form-control:focus {
            box-shadow: 0 0 0 3px rgba(181, 154, 59, 0.15);
            border-color: var(--accent-color);
            background: white;
        }

        .input-icon {
            position: absolute;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--accent-color);
            font-size: 1.2rem;
            transition: all 0.3s ease;
        }

        .password-toggle {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d;
            cursor: pointer;
            font-size: 1.1rem;
            transition: color 0.3s ease;
        }

        .password-toggle:hover {
            color: var(--accent-color);
        }

        .login-btn {
            background: linear-gradient(to right, var(--accent-color), var(--accent-light));
            border: none;
            border-radius: 4px;
            padding: 14px;
            font-weight: 600;
            font-size: 1.1rem;
            color: white;
            box-shadow: 0 4px 15px rgba(181, 154, 59, 0.3);
            transition: all 0.3s ease;
            margin-top: 15px;
        }

        .login-btn:hover {
            background: linear-gradient(to right, var(--accent-light), var(--accent-color));
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(181, 154, 59, 0.4);
        }

        .login-btn:active {
            transform: translateY(0);
        }

        .login-footer {
            text-align: center;
            margin-top: 30px;
            color: #6c757d;
            font-size: 0.9rem;
        }

        .login-footer a {
            color: var(--accent-color);
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s;
        }

        .login-footer a:hover {
            color: var(--primary-color);
        }

        .error-message {
            color: var(--error-color);
            background-color: rgba(163, 50, 11, 0.1);
            border-radius: 4px;
            padding: 10px;
            text-align: center;
            margin-top: 20px;
            font-weight: 500;
            display: none;
            animation: fade-in 0.3s ease-in-out;
        }

        .error-message.visible {
            display: block;
        }

        @keyframes fade-in {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Gold separator line */
        .gold-separator {
            height: 1px;
            background: linear-gradient(to right, transparent, var(--accent-color), transparent);
            margin: 30px 0;
        }

        /* Remember me checkbox */
        .remember-me {
            display: flex;
            align-items: center;
            margin-top: 15px;
        }

        .custom-checkbox {
            width: 18px;
            height: 18px;
            margin-right: 10px;
            accent-color: var(--accent-color);
        }

        .remember-label {
            color: #6c757d;
            font-size: 0.9rem;
        }

        @media (max-width: 850px) {
            .login-container {
                width: 95%;
                flex-direction: column;
            }

            .login-image {
                height: 200px;
            }

            .login-form-container {
                padding: 30px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-image">
            <div class="login-image-overlay">
                <p class="login-quote">"Education is not the learning of facts, but the training of the mind to think."</p>
                <p class="quote-author">— Albert Einstein</p>
            </div>
        </div>
        <div class="login-form-container">
            <div class="login-logo">
                <div class="logo-image">
                    <i class="bi bi-mortarboard-fill"></i>
                </div>
            </div>
            <div class="login-header">
                <h2>Student Portal</h2>
                <p>Sign in to your academic account</p>
            </div>
            <form action="login.php" method="post" id="loginForm">
                <div class="form-group">
                    <input type="text" class="form-control" id="reg_no" name="reg_no" placeholder="Registration Number" required>
                    <i class="bi bi-person-badge input-icon"></i>
                </div>
                <div class="form-group">
                    <input type="password" class="form-control" id="password" name="password" placeholder="Password" required>
                    <i class="bi bi-shield-lock input-icon"></i>
                    <i class="bi bi-eye-fill password-toggle" id="togglePassword"></i>
                </div>
                <div class="remember-me">
                    <input type="checkbox" class="custom-checkbox" id="remember" name="remember">
                    <label class="remember-label" for="remember">Keep me signed in</label>
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn login-btn" id="loginButton">
                        Sign In
                    </button>
                </div>
                <div id="error" class="error-message"></div>
                <div class="gold-separator"></div>
                <div class="login-footer">
                    <p>Forgot your password? <a href="#">Reset Password</a></p>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Toggle password visibility
        document.getElementById('togglePassword').addEventListener('click', function() {
            const passwordInput = document.getElementById('password');
            const toggleIcon = this;
            
            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                toggleIcon.classList.remove('bi-eye-fill');
                toggleIcon.classList.add('bi-eye-slash-fill');
            } else {
                passwordInput.type = 'password';
                toggleIcon.classList.remove('bi-eye-slash-fill');
                toggleIcon.classList.add('bi-eye-fill');
            }
        });

        // Form validation and submission
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            e.preventDefault(); // Prevent default form submission
            
            const regNo = document.getElementById('reg_no').value;
            const password = document.getElementById('password').value;
            const errorDiv = document.getElementById('error');
            const loginButton = document.getElementById('loginButton');

            // Disable button during processing
            loginButton.disabled = true;
            loginButton.innerHTML = 'Authenticating...';

            // Basic client-side validation
            if (!regNo || !password) {
                errorDiv.textContent = 'Please enter both Registration Number and Password';
                errorDiv.classList.add('visible');
                
                // Re-enable button
                loginButton.disabled = false;
                loginButton.innerHTML = 'Sign In';
                
                // Hide error message after 3 seconds
                setTimeout(() => {
                    errorDiv.classList.remove('visible');
                }, 3000);
            } else {
                errorDiv.textContent = '';
                errorDiv.classList.remove('visible');
                
                // Submit the form
                this.submit();
            }
        });

        // Subtle animation for form elements on page load
        window.addEventListener('DOMContentLoaded', () => {
            const formElements = document.querySelectorAll('.form-group, .login-btn, .login-header, .login-logo');
            
            formElements.forEach((element, index) => {
                element.style.opacity = '0';
                element.style.transform = 'translateY(20px)';
                element.style.transition = `all 0.5s ease ${index * 0.1}s`;
                
                setTimeout(() => {
                    element.style.opacity = '1';
                    element.style.transform = 'translateY(0)';
                }, 100);
            });
        });
    </script>
</body>
</html>



//login.php

<?php
session_start();
$conn = new mysqli("localhost", "studentuser", "studentpass", "studentdb");

// Check connection
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

// Get user input (with basic sanitization)
$reg_no = $conn->real_escape_string($_POST['reg_no']);
$password = $_POST['password'];

// Check password (hardcoded for now - you should use password_hash() in production)
if ($password === "student123") {
    // Fetch student data using prepared statement to prevent SQL injection
    $sql = "SELECT s.name, s.reg_no, p.profile_picture, p.address, p.phone_number as phone 
            FROM student_marks s
            LEFT JOIN dashboard p ON s.reg_no = p.reg_no
            WHERE s.reg_no = ?";
    
    $stmt = $conn->prepare($sql);
    $stmt->bind_param("s", $reg_no);
    $stmt->execute();
    $result = $stmt->get_result();
    
    if ($result->num_rows > 0) {
        $_SESSION['student_data'] = $result->fetch_assoc();
        header("Location: dashboard.php");
        exit();
    } else {
        die("Student not found!");
    }
} else {
    echo "Invalid credentials!";
}
$conn->close();
?>


//marks display.php

<?php
$conn = new mysqli("localhost", "studentuser", "studentpass", "studentdb");
$reg_no = $_SESSION['student_data']['reg_no'];
$sql = "SELECT * FROM student_marks WHERE reg_no = '$reg_no'";
$result = $conn->query($sql);

if ($result->num_rows > 0) {
    $row = $result->fetch_assoc();
    
    // Determine grade color and ranking
    $grade_color = match($row['grade']) {
        'A+' => 'success',
        'A' => 'success',
        'B+' => 'primary',
        'B' => 'info',
        'C+' => 'warning',
        'C' => 'warning',
        'D' => 'danger',
        'F' => 'danger',
        default => 'secondary'
    };

    $performance_message = match(true) {
        $row['percentage'] >= 90 => 'Excellent Performance! Outstanding achievement!',
        $row['percentage'] >= 80 => 'Great Job! Keep up the good work!',
        $row['percentage'] >= 70 => 'Good Performance. You can improve.',
        $row['percentage'] >= 60 => 'Satisfactory. Focus on weak areas.',
        default => 'Needs Significant Improvement'
    };
?>
<!DOCTYPE html>
<html>
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .academic-card {
            background: linear-gradient(145deg, #f4f9ff, #e6f2ff);
            border-radius: 15px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            padding: 25px;
            max-width: 600px;
            margin: 20px auto;
        }
        .subject-progress {
            height: 30px;
            background-color: #e9ecef;
            border-radius: 15px;
            overflow: hidden;
            margin-bottom: 15px;
        }
        .progress-bar {
            transition: width 1s ease-in-out;
        }
        .grade-badge {
            font-size: 1.2em;
            padding: 10px 15px;
        }
        .performance-message {
            background-color: rgba(0,123,255,0.1);
            border-left: 5px solid #007bff;
            padding: 15px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="academic-card">
            <h2 class="text-center mb-4">Academic Performance</h2>
            
            <div class="row">
                <div class="col-md-8">
                    <h4>Subject Marks</h4>
                    <div class="subject-progress mb-3">
                        <div class="progress-bar bg-primary" role="progressbar" 
                             style="width: <?php echo $row['subject1']; ?>%" 
                             aria-valuenow="<?php echo $row['subject1']; ?>" 
                             aria-valuemin="0" aria-valuemax="100">
                            Subject 1: <?php echo $row['subject1']; ?>%
                        </div>
                    </div>
                    <div class="subject-progress mb-3">
                        <div class="progress-bar bg-success" role="progressbar" 
                             style="width: <?php echo $row['subject2']; ?>%" 
                             aria-valuenow="<?php echo $row['subject2']; ?>" 
                             aria-valuemin="0" aria-valuemax="100">
                            Subject 2: <?php echo $row['subject2']; ?>%
                        </div>
                    </div>
                    <div class="subject-progress mb-3">
                        <div class="progress-bar bg-warning" role="progressbar" 
                             style="width: <?php echo $row['subject3']; ?>%" 
                             aria-valuenow="<?php echo $row['subject3']; ?>" 
                             aria-valuemin="0" aria-valuemax="100">
                            Subject 3: <?php echo $row['subject3']; ?>%
                        </div>
                    </div>
                </div>
                
                <div class="col-md-4 text-center">
                    <h4>Summary</h4>
                    <span class="badge bg-<?php echo $grade_color; ?> grade-badge">
                        Grade: <?php echo $row['grade']; ?>
                    </span>
                    <p class="mt-3">
                        <strong>Overall Percentage:</strong><br>
                        <span class="text-primary h4"><?php echo $row['percentage']; ?>%</span>
                    </p>
                </div>
            </div>
            
            <div class="performance-message">
                <h5>Performance Insight</h5>
                <p><?php echo $performance_message; ?></p>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
<?php
} else {
    echo '<div class="container mt-5"><div class="alert alert-warning text-center">No marks found!</div></div>';
}
$conn->close();
?>


//results.php

<?php
session_start();
if (!isset($_SESSION['reg_no'])) {
    header("Location: index.html");
    exit();
}

$servername = "localhost";
$username = "studentuser";
$password = "studentpass";
$dbname = "studentdb";

// Create connection
$conn = new mysqli($servername, $username, $password, $dbname);

// Check connection
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

$reg_no = $_SESSION['reg_no'];
$sql = "SELECT * FROM student_marks WHERE reg_no = '$reg_no' LIMIT 1";
$result = $conn->query($sql);

if ($result->num_rows > 0) {
    $row = $result->fetch_assoc();
} else {
    die("No records found");
}
$conn->close();
?>

<!DOCTYPE html>
<html>
<head>
    <title>Student Results</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .result-card { border: 1px solid #ddd; padding: 20px; max-width: 500px; margin: 0 auto; }
        table { width: 100%; border-collapse: collapse; margin: 20px 0; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .summary { margin-top: 20px; font-weight: bold; }
    </style>
</head>
<body>
    <div class="result-card">
        <h2>Student Result</h2>
        <p><strong>Name:</strong> <?php echo htmlspecialchars($row['name']); ?></p>
        <p><strong>Registration No:</strong> <?php echo htmlspecialchars($row['reg_no']); ?></p>
        
        <table>
            <tr>
                <th>Subject</th>
                <th>Marks</th>
            </tr>
            <tr>
                <td>Subject 1</td>
                <td><?php echo $row['subject1']; ?></td>
            </tr>
            <tr>
                <td>Subject 2</td>
                <td><?php echo $row['subject2']; ?></td>
            </tr>
            <tr>
                <td>Subject 3</td>
                <td><?php echo $row['subject3']; ?></td>
            </tr>
        </table>
        
        <div class="summary">
            <p>Percentage: <?php echo number_format($row['percentage'], 2); ?>%</p>
            <p>Grade: <?php echo $row['grade']; ?></p>
        </div>
        
        <a href="logout.php">Logout</a>
    </div>
</body>
</html>







//dashboard.php

<?php
session_start();

// Debug session
error_log("Session data at dashboard start: " . print_r($_SESSION, true));

if (!isset($_SESSION['student_data'])) {
    header("Location: index.html");
    exit();
}

$student = $_SESSION['student_data'];

// Display upload status messages
$upload_status = '';
if (isset($_GET['upload'])) {
    if ($_GET['upload'] == 'success') {
        $upload_status = '<div class="alert alert-success">Profile picture updated successfully!</div>';
    } elseif (isset($_GET['error'])) {
        $error_messages = [
            'invalidfiletype' => 'Invalid file type. Only JPG, JPEG, PNG, GIF are allowed.',
            'filetoobig' => 'File is too large. Maximum size is 5MB.',
            'uploadfailed' => 'Failed to upload profile picture.',
            'movefailed' => 'Failed to save the uploaded file.',
            'notimage' => 'The selected file is not an image.',
            'nofile' => 'No file was selected for upload.'
        ];
        $upload_status = '<div class="alert alert-danger">' . $error_messages[$_GET['error']] . '</div>';
    }
}
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #1c3d5a;
            --secondary-color: #ad8e63;
            --accent-color: #d1b48c;
            --background-light: #f9f7f4;
            --text-dark: #2a2b2a;
            --gray-light: #e6e2dd;
            --card-shadow: 0 10px 30px rgba(0,0,0,0.08);
            --subtle-gradient: linear-gradient(135deg, #f9f7f4, #f3eee8);
        }

        body {
            background-color: var(--background-light);
            font-family: 'Montserrat', sans-serif;
            color: var(--text-dark);
            min-height: 100vh;
        }

        h1, h2, h3, h4, .sidebar-brand {
            font-family: 'Playfair Display', serif;
        }

        .dashboard-container {
            display: flex;
            min-height: 100vh;
            background-color: white;
            box-shadow: var(--card-shadow);
        }

        .sidebar {
            width: 300px;
            background: linear-gradient(135deg, var(--primary-color), #0f2942);
            color: white;
            padding: 40px 25px;
            position: relative;
            transition: all 0.3s ease;
        }

        .sidebar-brand {
            text-align: center;
            margin-bottom: 50px;
            font-size: 1.9rem;
            font-weight: 700;
            letter-spacing: 0.5px;
            padding-bottom: 20px;
            border-bottom: 1px solid rgba(255,255,255,0.15);
        }

        .student-quick-info {
            display: flex;
            align-items: center;
            padding: 20px 0;
            margin-bottom: 40px;
        }

        .sidebar-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid var(--accent-color);
            margin-right: 18px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }

        .student-name {
            font-size: 1rem;
            font-weight: 600;
            margin: 0;
            letter-spacing: 0.5px;
        }

        .student-id {
            font-size: 0.85rem;
            opacity: 0.8;
            margin: 0;
            letter-spacing: 0.5px;
        }

        .sidebar-menu {
            list-style: none;
            padding: 0;
            margin-top: 30px;
        }

        .sidebar-menu li {
            margin-bottom: 20px;
        }

        .sidebar-menu a {
            color: white;
            text-decoration: none;
            display: flex;
            align-items: center;
            padding: 14px 22px;
            border-radius: 12px;
            transition: all 0.3s ease;
            font-weight: 500;
            letter-spacing: 0.5px;
        }

        .sidebar-menu a:hover, .sidebar-menu a.active {
            background-color: rgba(255,255,255,0.15);
            transform: translateX(5px);
        }

        .sidebar-menu a i {
            font-size: 1.3rem;
            margin-right: 15px;
        }

        .main-content {
            flex: 1;
            padding: 50px;
            overflow-y: auto;
            background-color: var(--background-light);
        }

        .content-header {
            margin-bottom: 40px;
        }

        .content-header h1 {
            font-size: 2.2rem;
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: 10px;
        }

        .breadcrumb {
            font-size: 0.9rem;
            font-weight: 500;
        }

        .breadcrumb-item a {
            color: var(--secondary-color);
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .breadcrumb-item a:hover {
            color: var(--primary-color);
        }

        .profile-header {
            display: flex;
            align-items: center;
            background-color: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: var(--card-shadow);
            margin-bottom: 40px;
            position: relative;
            overflow: hidden;
        }

        .profile-header::after {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 180px;
            height: 100%;
            background: linear-gradient(135deg, rgba(28, 61, 90, 0.05), rgba(173, 142, 99, 0.08));
            clip-path: polygon(100% 0, 0 0, 100% 100%);
        }

        .profile-pic {
            width: 180px;
            height: 180px;
            border-radius: 50%;
            object-fit: cover;
            border: 5px solid white;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            margin-right: 50px;
            transition: transform 0.4s ease, box-shadow 0.4s ease;
        }

        .profile-pic:hover {
            transform: scale(1.05);
            box-shadow: 0 15px 35px rgba(0,0,0,0.15);
        }

        .profile-details {
            flex: 1;
        }

        .profile-details h2 {
            color: var(--primary-color);
            margin-bottom: 25px;
            font-weight: 700;
            font-size: 2.2rem;
            position: relative;
            padding-bottom: 15px;
        }

        .profile-details h2::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 80px;
            height: 3px;
            background: var(--secondary-color);
        }

        .profile-info {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
            gap: 25px;
        }

        .info-item {
            margin-bottom: 15px;
        }

        .info-label {
            font-size: 0.9rem;
            font-weight: 600;
            color: #7a7a7a;
            margin-bottom: 8px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .info-value {
            font-size: 1.15rem;
            font-weight: 500;
            color: var(--text-dark);
        }

        .card {
            background-color: white;
            border-radius: 20px;
            box-shadow: var(--card-shadow);
            padding: 40px;
            margin-bottom: 40px;
            border: none;
            transition: transform 0.4s ease, box-shadow 0.4s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 25px 35px -5px rgba(0,0,0,0.1);
        }

        .card-title {
            font-size: 1.6rem;
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: 30px;
            display: flex;
            align-items: center;
            position: relative;
            padding-bottom: 15px;
        }

        .card-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 60px;
            height: 2px;
            background: var(--secondary-color);
        }

        .card-title i {
            margin-right: 18px;
            font-size: 1.6rem;
            background-color: rgba(28, 61, 90, 0.06);
            padding: 12px;
            border-radius: 12px;
            color: var(--primary-color);
        }

        .btn-primary {
            background-color: var(--secondary-color);
            border: none;
            border-radius: 12px;
            padding: 14px 30px;
            font-weight: 600;
            transition: all 0.3s ease;
            letter-spacing: 0.5px;
            color: white;
        }

        .btn-primary:hover {
            background-color: var(--primary-color);
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(28, 61, 90, 0.2);
        }

        .alert {
            border-radius: 15px;
            padding: 18px 25px;
            margin-bottom: 30px;
            border: none;
            font-weight: 500;
        }

        .alert-success {
            background-color: rgba(60, 201, 167, 0.15);
            color: #0f856e;
        }

        .alert-danger {
            background-color: rgba(234, 84, 85, 0.15);
            color: #d63031;
        }

        .form-control {
            border-radius: 12px;
            padding: 14px 18px;
            border: 2px solid var(--gray-light);
            transition: all 0.3s ease;
            font-size: 0.95rem;
        }

        .form-control:focus {
            border-color: var(--secondary-color);
            box-shadow: 0 0 0 4px rgba(173, 142, 99, 0.15);
        }

        /* Upload button design */
        .file-upload {
            position: relative;
            display: inline-block;
            width: 100%;
        }

        .file-upload-label {
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f5f2ee;
            border: 2px dashed var(--secondary-color);
            border-radius: 15px;
            padding: 35px;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-bottom: 25px;
        }

        .file-upload-label:hover {
            background-color: #efe9e2;
            border-color: var(--primary-color);
        }

        .file-upload-icon {
            font-size: 2.2rem;
            color: var(--secondary-color);
            margin-right: 18px;
        }

        .file-upload-text {
            font-weight: 500;
            color: #666;
        }

        #profile_picture {
            position: absolute;
            left: 0;
            top: 0;
            right: 0;
            bottom: 0;
            opacity: 0;
            width: 100%;
            cursor: pointer;
        }

        .form-text {
            color: #888;
            font-size: 0.9rem;
            margin-bottom: 25px;
        }

        /* Section styling */
        section {
            animation: fadeIn 0.5s ease-in-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Responsive adjustments */
        @media (max-width: 1200px) {
            .sidebar {
                width: 280px;
                padding: 30px 20px;
            }
            .main-content {
                padding: 40px;
            }
        }

        @media (max-width: 992px) {
            .sidebar {
                width: 260px;
            }
            .profile-header {
                flex-direction: column;
                text-align: center;
                padding: 30px;
            }
            .profile-pic {
                margin-right: 0;
                margin-bottom: 30px;
            }
            .profile-details h2::after {
                left: 50%;
                transform: translateX(-50%);
            }
            .profile-info {
                grid-template-columns: 1fr;
            }
            .info-item {
                text-align: center;
            }
        }

        @media (max-width: 768px) {
            .dashboard-container {
                flex-direction: column;
            }
            .sidebar {
                width: 100%;
                padding: 25px;
            }
            .main-content {
                padding: 25px;
            }
            .student-quick-info {
                justify-content: center;
            }
            .card {
                padding: 25px;
            }
            .card-title {
                font-size: 1.4rem;
            }
            .content-header h1 {
                font-size: 1.8rem;
            }
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="sidebar-brand">
                <i class="bi bi-mortarboard me-2"></i>
                Student Portal
            </div>
            
            <div class="student-quick-info">
                <?php if (!empty($student['profile_picture']) && file_exists("uploads/" . $student['profile_picture'])): ?>
                    <img src="uploads/<?php echo htmlspecialchars($student['profile_picture']); ?>" class="sidebar-avatar" alt="Profile Picture">
                <?php else: ?>
                    <img src="uploads/default.jpg" class="sidebar-avatar" alt="Default Profile Picture">
                <?php endif; ?>
                <div>
                    <h6 class="student-name"><?php echo htmlspecialchars($student['name']); ?></h6>
                    <p class="student-id"><?php echo htmlspecialchars($student['reg_no']); ?></p>
                </div>
            </div>
            
            <ul class="sidebar-menu">
                <li>
                    <a href="#" class="active" onclick="showSection('profile')">
                        <i class="bi bi-person-circle"></i> Profile
                    </a>
                </li>
                <li>
                    <a href="#" onclick="showSection('marks')">
                        <i class="bi bi-bar-chart-line"></i> Academic Performance
                    </a>
                </li>
                <li>
                    <a href="#" onclick="showSection('calendar')">
                        <i class="bi bi-calendar-week"></i> Calendar
                    </a>
                </li>
                <li>
                    <a href="#" onclick="showSection('courses')">
                        <i class="bi bi-book"></i> Courses
                    </a>
                </li>
                <li>
                    <a href="logout.php">
                        <i class="bi bi-box-arrow-right"></i> Logout
                    </a>
                </li>
            </ul>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <!-- Profile Section -->
            <section id="profile-section">
                <div class="content-header">
                    <h1>My Profile</h1>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="#">Dashboard</a></li>
                            <li class="breadcrumb-item active" aria-current="page">Profile</li>
                        </ol>
                    </nav>
                </div>

                <?php echo $upload_status; ?>

                <div class="profile-header">
                    <?php if (!empty($student['profile_picture']) && file_exists("uploads/" . $student['profile_picture'])): ?>
                        <img src="uploads/<?php echo htmlspecialchars($student['profile_picture']); ?>" class="profile-pic" alt="Profile Picture">
                    <?php else: ?>
                        <img src="uploads/default.jpg" class="profile-pic" alt="Default Profile Picture">
                    <?php endif; ?>
                    
                    <div class="profile-details">
                        <h2><?php echo htmlspecialchars($student['name']); ?></h2>
                        <div class="profile-info">
                            <div class="info-item">
                                <div class="info-label">Registration No</div>
                                <div class="info-value"><?php echo htmlspecialchars($student['reg_no']); ?></div>
                            </div>
                            <div class="info-item">
                                <div class="info-label">Address</div>
                                <div class="info-value"><?php echo htmlspecialchars($student['address']); ?></div>
                            </div>
                            <div class="info-item">
                                <div class="info-label">Phone</div>
                                <div class="info-value"><?php echo htmlspecialchars($student['phone']); ?></div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="card">
                    <h3 class="card-title"><i class="bi bi-camera"></i>Update Profile Picture</h3>
                    <form action="upload.php" method="post" enctype="multipart/form-data">
                        <div class="file-upload">
                            <label class="file-upload-label">
                                <span class="file-upload-icon"><i class="bi bi-cloud-arrow-up"></i></span>
                                <span class="file-upload-text">Drag & drop your photo or click to browse</span>
                                <input type="file" class="form-control" name="profile_picture" id="profile_picture" accept="image/*" required>
                            </label>
                        </div>
                        <div class="form-text mb-4">Accepted formats: JPG, JPEG, PNG, GIF (Max size: 5MB)</div>
                        <button type="submit" class="btn btn-primary" name="upload">
                            <i class="bi bi-check-circle me-2"></i>Update Profile Picture
                        </button>
                    </form>
                </div>

                <div class="card">
                    <h3 class="card-title"><i class="bi bi-person-lines-fill"></i>Personal Information</h3>
                    <div class="row">
                        <div class="col-md-6 mb-4">
                            <div class="info-label">Full Name</div>
                            <div class="info-value"><?php echo htmlspecialchars($student['name']); ?></div>
                        </div>
                        <div class="col-md-6 mb-4">
                            <div class="info-label">Registration Number</div>
                            <div class="info-value"><?php echo htmlspecialchars($student['reg_no']); ?></div>
                        </div>
                        <div class="col-md-6 mb-4">
                            <div class="info-label">Address</div>
                            <div class="info-value"><?php echo htmlspecialchars($student['address']); ?></div>
                        </div>
                        <div class="col-md-6 mb-4">
                            <div class="info-label">Phone Number</div>
                            <div class="info-value"><?php echo htmlspecialchars($student['phone']); ?></div>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Marks Section -->
            <section id="marks-section" style="display:none;">
                <div class="content-header">
                    <h1>Academic Performance</h1>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="#">Dashboard</a></li>
                            <li class="breadcrumb-item active" aria-current="page">Academic Performance</li>
                        </ol>
                    </nav>
                </div>
                
                <div class="card">
                    <h3 class="card-title"><i class="bi bi-bar-chart-line"></i>Performance Overview</h3>
                    <?php include 'marks_display.php'; ?>
                </div>
            </section>

            <!-- Calendar Section (placeholder) -->
            <section id="calendar-section" style="display:none;">
                <div class="content-header">
                    <h1>Academic Calendar</h1>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="#">Dashboard</a></li>
                            <li class="breadcrumb-item active" aria-current="page">Calendar</li>
                        </ol>
                    </nav>
                </div>
                
                <div class="card">
                    <h3 class="card-title"><i class="bi bi-calendar-week"></i>Upcoming Events</h3>
                    <p class="text-muted">No upcoming events found.</p>
                </div>
            </section>

            <!-- Courses Section (placeholder) -->
            <section id="courses-section" style="display:none;">
                <div class="content-header">
                    <h1>My Courses</h1>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="#">Dashboard</a></li>
                            <li class="breadcrumb-item active" aria-current="page">Courses</li>
                        </ol>
                    </nav>
                </div>
                
                <div class="card">
                    <h3 class="card-title"><i class="bi bi-book"></i>Enrolled Courses</h3>
                    <p class="text-muted">No active courses found.</p>
                </div>
            </section>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function showSection(section) {
            // Hide all sections
            document.getElementById('profile-section').style.display = 'none';
            document.getElementById('marks-section').style.display = 'none';
            document.getElementById('calendar-section').style.display = 'none';
            document.getElementById('courses-section').style.display = 'none';
            
            // Remove active class from all menu items
            document.querySelectorAll('.sidebar-menu a').forEach(item => {
                item.classList.remove('active');
            });
            
            // Show selected section and highlight menu item
            if (section === 'profile') {
                document.getElementById('profile-section').style.display = 'block';
                document.querySelector('.sidebar-menu a[onclick="showSection(\'profile\')"]').classList.add('active');
            } else if (section === 'marks') {
                document.getElementById('marks-section').style.display = 'block';
                document.querySelector('.sidebar-menu a[onclick="showSection(\'marks\')"]').classList.add('active');
            } else if (section === 'calendar') {
                document.getElementById('calendar-section').style.display = 'block';
                document.querySelector('.sidebar-menu a[onclick="showSection(\'calendar\')"]').classList.add('active');
            } else if (section === 'courses') {
                document.getElementById('courses-section').style.display = 'block';
                document.querySelector('.sidebar-menu a[onclick="showSection(\'courses\')"]').classList.add('active');
            }
        }
        
        // File input preview logic
        document.getElementById('profile_picture').addEventListener('change', function(e) {
            const fileName = e.target.files[0].name;
            document.querySelector('.file-upload-text').textContent = fileName;
        });
    </script>
</body>
</html>



//logout.php


<?php
session_start();
session_unset(); // Clear all session data
session_destroy();
header("Location: index.html");
exit();
?>
